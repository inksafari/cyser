--yes-playlist
--output "[%(uploader)s] - %(title)s-%(resolution)s-%(fps)sfps-%(id)s.%(ext)s"
--continue
--ignore-errors
--format "bestvideo[ext=mp4]+bestaudio[ext=m4a]/best[ext=mp4]/best/best"

# aria2 / axel
--external-downloader axel
